syntax = "proto2";

package cooboc.proto;

message VehicleResponseTopic {
    optional bool  isValid       = 1 [default = false];
    repeated float wheelOdometry = 2;
    repeated float wheelSpeed    = 3;
}
