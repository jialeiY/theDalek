syntax = "proto2";

package cooboc.proto;

enum CurvatureDistribution {
    CONSIDER_PREVIOUS = 0;
    CONSIDER_NEXT     = 1;
    CONSIDER_BOTH     = 2;
    CONSTANT_NEXT     = 3;
    DONT_CARE         = 4;
}
